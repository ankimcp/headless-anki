services:
  anki:
    image: headless-anki:addons
    volumes:
      - ./data/prefs21.db:/data/prefs21.db
      # Mount custom addons like this:
      # - ./my-addon:/data/addons21/12345678
    ports:
      - "5900:5900"
